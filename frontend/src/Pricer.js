import React, { Component } from "react";

class Pricer extends Component {
  render() {
    return (
      <div>
        <h2>Pricer</h2>
        <p>Pricing page, and bulk add </p>
      </div>
    );
  }
}

export default Pricer;
